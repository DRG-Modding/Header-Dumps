enum class EOculusMR_BoundaryType
{
    BT_OuterBoundary = 0,
    BT_PlayArea = 1,
    BT_MAX = 2,
}

enum class EOculusMR_CompositionMethod
{
    ExternalComposition = 0,
    DirectComposition = 1,
    EOculusMR_MAX = 2,
}

enum class EOculusMR_DepthQuality
{
    DQ_Low = 0,
    DQ_Medium = 1,
    DQ_High = 2,
    DQ_MAX = 3,
}

enum class EOculusMR_PostProcessEffects
{
    PPE_Off = 0,
    PPE_On = 1,
    PPE_MAX = 2,
}

enum class EOculusMR_VirtualGreenScreenType
{
    VGS_Off = 0,
    VGS_OuterBoundary = 1,
    VGS_PlayArea = 2,
    VGS_MAX = 3,
}

enum class EOculusMR_ClippingReference
{
    CR_TrackingReference = 0,
    CR_Head = 1,
    CR_MAX = 2,
}

enum class EOculusMR_CameraDeviceEnum
{
    CD_None = 0,
    CD_WebCamera0 = 1,
    CD_WebCamera1 = 2,
    CD_ZEDCamera = 3,
    CD_MAX = 4,
}

